<h1><%= @patient.name %></h1>

<ul>
    <% @patient.appointments.each do |a| %>
        <li>Doctor: <%= link_to a.doctor.name, doctor_path(a.doctor) %>, 
        Time: <%= a.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></li>
    <% end %>
</ul>